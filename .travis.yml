language: cpp
notifications:
  email: false

matrix:
  include:
    - os: linux
      compiler: gcc
      addons:
        apt:
          sources: ['george-edison55-precise-backports']
          packages: ['cmake-data', 'cmake']
      env:

    - os: osx
      compiler: gcc
      language: generic
      env:
        - COMPILER=g++
        - COMP=gcc
        - PYTHON=3.5.1

script:
      - cmake .
      - make
      - cd tests
      - pip2 install --user python-chess futures
      - python ci.py
